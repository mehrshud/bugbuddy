#!/usr/bin/env python
from setuptools import setup

setup(
    name='bugbuddy',
    version='1.0',
    packages=['bugbuddy'],
    install_requires=[
        # list your dependencies here, e.g.
        'requests',
        'numpy'
    ]
)
